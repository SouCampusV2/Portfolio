# Ideas backlog

Things to add later. Not built yet.

## Site

- **Skill tooltips.** On hover over a technology chip, show how well I know it (level) and how long / how recently I've used it. Needs a level + "since" / "last used" per item in `site.tech`.
- **Visitor avatars as crystal knights.** A "Visited by N people" pill with small avatars, where each visitor gets a generated crystal-knight avatar (sigma style). Needs a visit counter (e.g. a KV store on Vercel).
- **Avatar in the hero** that swaps between an art version and a real photo on hover (like renlenon.vercel.app). Waiting on the photo.
- **Real photos** for "Outside the IDE" (set `photo` on each hobby in `content/site.ts`).
- **More case studies**: Crossdesk, CMI / proceedit.
- **Bachelor's thesis link** on the n8n onboarding card, once the public record (UT DSpace) is confirmed and proceedit is fine with it.

## Beyond the site

- **SouCampus file-check service in Python**: a separate service that validates uploaded map files (archive safety, real Minecraft world/structure, duplicates) before a listing goes live. Add it to the portfolio once it ships.
- **GitHub profile README** in neofetch style (ASCII portrait + key/value info), like the TikTok example.

## Decided against

- AI chat widget ("Chat with …"): not wanted.
